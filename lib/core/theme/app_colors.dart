import 'package:flutter/material.dart';

class AppColors {
  // 기본 색상
  static const primary = Color(0xFFFF7A00); // 주황색
  static const primaryLight = Color(0xFFFF9433);
  static const primaryDark = Color(0xFFE56C00);
  
  // 중립 색상
  static const darkGray = Color(0xFF2D2D2D);
  static const mediumGray = Color(0xFF757575);
  static const lightGray = Color(0xFFF5F5F5);
  static const white = Color(0xFFFFFFFF);
  
  // 기능 색상
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFFC107);
  static const error = Color(0xFFF44336);
  static const info = Color(0xFF2196F3);
  
  // 배경 색상
  static const background = lightGray;
  static const cardBackground = white;
  
  // 텍스트 색상
  static const textPrimary = darkGray;
  static const textSecondary = mediumGray;
  static const textLight = Color(0xFFBDBDBD);
}